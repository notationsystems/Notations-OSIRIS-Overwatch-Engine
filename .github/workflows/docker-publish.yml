name: Publish Docker image

# Branch trigger corrected during the shipping order (S-1): the repository
# has never had a `master` branch — the trigger was inherited dead. The
# mainline is `main` from v0.1.0 onward; the tag trigger was always live.
on:
  push:
    branches: [main]
    tags: ["v*.*.*"]
  workflow_dispatch:

# Two quick pushes to master must not race to overwrite the `latest` tag.
concurrency:
  group: docker-publish-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      # arm64 is built under emulation — needed for CasaOS on ARM SBCs.
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,format=short

      - uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          # The image carries the commit it was built from, so a deployed
          # instance can be attributed to a tree rather than reporting
          # 'unstamped-build' honestly and uselessly.
          build-args: |
            SEA_DOG_BUILD_SHA=${{ github.sha }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
